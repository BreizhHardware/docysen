{
  "name": "thumbnail-worker",
  "sourceRoot": "apps/thumbnail-worker/src",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "test -x .venv/bin/python && .venv/bin/python -m src.main || python3 -m src.main",
        "cwd": "apps/thumbnail-worker"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "test -x .venv/bin/pytest && .venv/bin/pytest || pytest",
        "cwd": "apps/thumbnail-worker"
      }
    },
    "docker-build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build -t docysen/thumbnail-worker .",
        "cwd": "apps/thumbnail-worker"
      }
    }
  }
}
