info:
  name: List Promos
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiServiceUrl}}/promos"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true

docs: |-
  Peuple les selects promo/semestre du formulaire d'upload. Nécessite "Login" avant.
