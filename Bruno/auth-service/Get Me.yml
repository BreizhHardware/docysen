info:
  name: Get Me
  type: http
  seq: 3

http:
  method: GET
  url: "{{authServiceUrl}}/users/me"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true

docs: |-
  Nécessite d'avoir lancé "Login" avant (remplit {{token}}).
